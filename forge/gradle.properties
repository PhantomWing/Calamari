# This module builds against the Forge platform on 1.20.1 (Architectury Loom does
# not auto-detect the platform from the architectury { } block). The resulting jar
# also runs on NeoForge 1.20.1, which is a Forge-compatible soft-fork.
loom.platform=forge
